# Makefile for project-level tasks

# deps-install: Set the Rust toolchain to stable using rustup.
# This ensures the stable version of Rust is used for builds and development.
deps-install:
	@rustup default stable
